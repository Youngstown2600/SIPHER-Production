
Live Main call view: Alt+1 remains uncluttered while idle. During an active call it automatically adds `LIVE WIRE // CALL TAP` with the called/remote number, live elapsed time, call state, codec, local/remote RTP addresses, packet/loss/jitter/RTT/MOS data, mute/media state, and direct call-control commands. Main refreshes once per second during a call and restores the idle layout automatically after hangup.
# S.I.P.H.E.R. r17 UI identity

## Direction
Underground phone-phreak / signal lab. Carrier-access rail, hard-edged monospace GUI, double-line CLI frames, PHREAK DECK navigation and `phreak>` prompt.

## Shared themes (10)
System, Midnight, Slate, Ocean, Arctic, Solarized, Monochrome, Cobalt, Amber, High Contrast

## S.I.P.H.E.R.-only themes (10)
Black Ice, Night Vision, Blue Box, Red Box, 2600, WarGames, Phosphor, Cyberpunk, Blood Moon, Terminal Gold

## Preservation
The r15 behavior-bearing core remains checksum-pinned by `tests/core_r15_preservation_test.sh`.
